apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: devops
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s
    scrape_configs:
      - job_name: 'prometheus'
        static_configs:
          - targets: ['localhost:9090']

      - job_name: 'ubuntu-wsl'
        static_configs:
          # Utilise ton IP de passerelle WSL confirmée
          - targets: ['172.20.198.25:9100']
      
      - job_name: 'jenkins'
        metrics_path: '/prometheus/'
        static_configs:
          # Utilise ton IP d'hôte pour Jenkins
          - targets: ['172.20.198.25:8080']

      - job_name: 'spring-app'
        metrics_path: '/student/actuator/prometheus'
        static_configs:
          # Utilise le nom DNS interne de ton service Spring
          - targets: ['spring-service.devops.svc.cluster.local:8089']
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: monitoring-stack
  namespace: devops
spec:
  replicas: 1
  selector:
    matchLabels:
      app: monitoring
  template:
    metadata:
      labels:
        app: monitoring
    spec:
      containers:
        # --- PROMETHEUS ---
        - name: prometheus
          image: prom/prometheus:latest
          args:
            - "--config.file=/etc/prometheus/prometheus.yml"
            - "--storage.tsdb.path=/prometheus"
          ports:
            - containerPort: 9090
          volumeMounts:
            - name: prometheus-storage
              mountPath: /prometheus
            - name: config-volume
              mountPath: /etc/prometheus/prometheus.yml
              subPath: prometheus.yml

        # --- GRAFANA ---
        - name: grafana
          image: grafana/grafana:latest
          ports:
            - containerPort: 3000
          volumeMounts:
            - name: grafana-storage
              mountPath: /var/lib/grafana

      volumes:
        - name: prometheus-storage
          persistentVolumeClaim:
            claimName: prometheus-pvc
        - name: grafana-storage
          persistentVolumeClaim:
            claimName: grafana-pvc
        - name: config-volume
          configMap:
            name: prometheus-config
---
apiVersion: v1
kind: Service
metadata:
  name: monitoring-service
  namespace: devops
spec:
  type: NodePort
  selector:
    app: monitoring
  ports:
    - name: prometheus
      port: 9090
      targetPort: 9090
      nodePort: 30091
    - name: grafana
      port: 3000
      targetPort: 3000
      nodePort: 30031